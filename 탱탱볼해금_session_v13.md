# 탱탱볼해금 SESSION v13 — 새 버전 20260711-3 콘솔 등록 완료, 폰 테스트 대기

> 작성일: 2026-07-11 · 이전: `탱탱볼해금_session_v12.md` · 다음 세션은 **이 문서를 먼저** 읽을 것
> 요지: 화면 잘림 수정 버전 `20260711-3`을 앱인토스 콘솔에 등록 완료(검토 필요 상태). 검토 요청 버튼은 **사용자가 폰에서 테스트 버전을 1회 실행해야 활성화**된다. 광고는 콘솔 광고 승인 대기 중이라 이번 버전에 포함하지 않았다(테스트 ID 유지).

## 0. 콘솔 상태 (2026-07-11 01:46 등록)

| 항목 | 값 |
|---|---|
| 새 버전 | `20260711-3` / SDK 2.7.0 / **검토 필요** |
| 메모 | v3 — 화면 잘림 수정: 뷰포트 감지·밀림 복구, 메뉴 압축(낮은 화면 시작버튼 표시), 스크롤 허용, 터치 판정 정확화. 광고 테스트 ID·법정 표시 유지 |
| 현재 출시 버전 | `20260710-2` (2026-07-10 19:19 출시) |
| 검토 요청 버튼 | **비활성** — 새 버전 폰 1회 실행 후 활성화 |

콘솔: `https://apps-in-toss.toss.im/workspace/50627/mini-app/42268/app-build`

## 1. 다음 단계 (순서대로)

1. **사용자**: 콘솔 `20260711-3` 행의 `테스트` 버튼 → QR을 폰 카메라로 스캔 → 토스 앱에서 테스트 버전 실행.
2. 실기기 확인: ① 메뉴에서 **게임 시작 버튼이 보이는지** ② 메뉴/설정 스크롤 ③ 게임 좌/우 터치 정상(판정 로직 변경됨) ④ 상단 바가 생겨도 화면이 밀리지 않는지.
3. 문제 없으면 콘솔에서 `검토 요청` 클릭(활성화됨) — v9 규칙대로 사용자가 직접 클릭. 심사는 영업일 7일 내.
4. 승인 후 `출시하기` 클릭(사용자 직접) → 실기기 재확인.
5. 광고 승인이 나면: 실제 adGroupId 2개를 `app/src/services/ads.ts:21-24`에 교체 → 새 버전(v4)으로 같은 절차.

## 2. 이번 세션 코드 커밋 (모두 push 완료)

- `0a3e01a` 뷰포트 감지·밀림 복구·스크롤 허용 / `47e6c08` offset형 밀림 보정 / `cb28c08` 메뉴 압축+터치 판정 / `b624756` 크기만 축소되는 경우 보정
- 세션문서: `1ede721`(v12)
- 검증: tsc+vite 빌드 통과, headless Chrome 844×280/390 확인, `ait build` 성공

## 3. 주의사항

- 검토 제출 후에는 같은 버전을 수정할 수 없다. 문제 발견 시 새 번들로.
- 콘솔 메모는 **120자 제한**(초과 시 저장 거부 토스트).
- 콘솔 세션이 남아 있어 재로그인 없이 접속됐다(chrome-devtools MCP 프로필). 풀리면 사용자 로그인 필요.
- Codex P2 보류 3건은 v12 문서 참조(실기기 프레임 끊김 체감 시 적용).
