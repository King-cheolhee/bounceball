# 탱탱볼해금 SESSION v16 — 운영 광고 담은 20260711-4 등록 완료, 폰 테스트 대기

> 작성일: 2026-07-11 · 이전: `탱탱볼해금_session_v15.md` · 다음 세션은 **이 문서를 먼저** 읽을 것
> 요지: 화면 수정본 `20260711-3`이 **출시됨**(12:27). 이어서 전면형·보상형 **운영 광고 ID를 담은 새 버전 `20260711-4`를 콘솔에 등록**했고 현재 **검토 필요** 상태. 폰 테스트 QR을 띄운 상태이며, 사용자가 폰 1회 실행 후 `검토 요청`을 눌러야 심사가 시작된다.

## 0. 콘솔 상태 (2026-07-11)

| 버전 | 내용 | 상태 |
|---|---|---|
| `20260711-4` | 운영 광고(전면·보상) + 화면 수정 | **검토 필요** (폰 테스트 후 검토 요청 가능) |
| `20260711-3` | 화면 잘림 수정 | **현재 출시됨** (12:27 출시) |
| `20260710-2` | 브랜드 아이콘 | 출시됨(과거) |

콘솔: `https://apps-in-toss.toss.im/workspace/50627/mini-app/42268/app-build`
`20260711-4` 메모: "v4 — 인앱 광고 운영 ID 적용: 전면형·보상형 모두 콘솔 승인 운영 광고로 교체(테스트 ID 제거). 화면 수정·법정 표시는 v3 유지."

## 1. 다음 단계 (사용자 직접)

1. 콘솔 `20260711-4` 행 `테스트` → QR을 폰 카메라로 스캔 → 토스 앱에서 실행(검토 요청 버튼 활성화 조건).
2. 실기기 확인:
   - **전면형 광고**: 게임오버/스테이지 전환 시 실제 광고 노출·정상 종료.
   - **보상형 광고**: "광고 보고 부활" 시 시청 완료 후 **부활(보상) 실제 지급**(`userEarnedReward`).
   - 화면: 상단 바 밀림 없음, 메뉴/설정 스크롤, 좌/우 터치 정상.
3. 이상 없으면 콘솔 `검토 요청` 클릭(사용자 직접, v9 규칙). 심사는 영업일 7일 내.
4. 승인 후 `출시하기` 클릭(사용자 직접).

## 2. 이번 세션 코드/빌드

- 광고 커밋: `2bb664f`(전면형) → `8d5beac`(보상형). 두 ID 모두 `ait.v2.live.*` 운영.
- `.ait` 재빌드(운영 ID 포함) 후 콘솔 업로드로 `20260711-4` 등록. deploymentId `019f4f53-9132-747c-87d9-a078a44a76a2`.
- 검증: `npm run build` 통과, 빌드 산출물(`dist/web/assets/index-BurhUpzH.js`)에 운영 ID 2종 포함·테스트 ID 0 확인. Codex 교차검증 P1/P2 없음.

## 3. 주의사항

- **광고는 코드로 정상 여부 확인 불가** → 반드시 폰(샌드박스/실기기)에서 노출·보상 확인.
- 검토 요청 제출 후에는 같은 버전 수정 불가. 문제 시 새 번들(20260711-5).
- 배포 전 반드시 `.ait` 재빌드(`npx ait build`) — 디스크 번들이 코드 변경보다 오래됐을 수 있음(이번에도 재빌드로 해결).
- chrome-devtools MCP가 "already running"으로 붙지 못하면: MCP 프로필 크롬 반복 종료(0 확보) + 싱글턴 락 제거 후 재시도(이번 세션에서 이 방법으로 복구).
- 콘솔 메모 120자 제한.
