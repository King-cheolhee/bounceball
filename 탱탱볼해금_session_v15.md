# 탱탱볼해금 SESSION v15 — 전면형·보상형 광고 모두 운영 ID 적용 (코드만, 미배포)

> 작성일: 2026-07-11 · 이전: `탱탱볼해금_session_v14.md` · 다음 세션은 **이 문서를 먼저** 읽을 것
> 요지: v14에서 전면형만 운영 ID였으나, **보상형도 운영 ID로 교체 완료**. 이제 `ads.ts`에 테스트 광고 ID가 하나도 남지 않았다. 단, **아직 배포 전**이며 콘솔 심사 중 버전 `20260711-3`에는 광고 변경이 포함돼 있지 않다.

## 0. 현재 광고 ID 상태 (master 기준, 커밋 `8d5beac`)

| 광고 | 운영 ID | 상태 |
|---|---|---|
| 전면형(interstitial) | `ait.v2.live.b5a8766b61d547e4` | 운영 ID 적용 |
| 보상형(rewarded) | `ait.v2.live.55440ecc937e4613` | 운영 ID 적용 |

테스트 ID(`ait-ad-test-*`) 잔존 없음(grep 확인). 파일: `app/src/services/ads.ts:21-24`.

## 1. 심사 중 버전과의 차이 (v14와 동일 — 중요)

| 대상 | 광고 ID | 상태 |
|---|---|---|
| 콘솔 심사 중 `20260711-3` | 전면·보상 **모두 테스트 ID** | 검토 중(화면 잘림 수정만) |
| 로컬 master | 전면·보상 **모두 운영 ID** | **미배포** |

→ 다음 새 버전을 빌드하면 화면 수정 + 운영 광고(전면·보상)가 함께 들어간다.

## 2. 이번 세션에 한 일

- (v14) 전면형 운영 ID 교체 `2bb664f`.
- (v15) 보상형 운영 ID 교체 `8d5beac` — 주석도 두 광고 모두 운영으로 갱신.
- 검증: `npm run build`(tsc+vite) 통과. Codex(GPT-5.6-sol xhigh) 교차검증 **P1/P2 없음**.

## 3. 다음 단계 (사용자 결정 필요 — v14와 동일)

`20260711-3`(화면 수정)이 검토 중이라 광고 반영엔 **새 버전** 필요:

- **A안(권장): 심사 결과 대기** 후 master(화면+운영광고) 기준 새 `.ait` 빌드→등록→폰 테스트→검토 요청.
- **B안: 지금 심사 취소 후 하나로 묶기** — **검토 취소는 사용자 승인 필수**, 임의 취소 안 함.

배포: `app`에서 `npx ait deploy -m "..."` 또는 콘솔 `버전 등록`에 `app/tangtangball.ait` 업로드. 콘솔 메모 **120자 제한**.

## 4. 반드시 실기기 확인할 것 (운영 ID는 코드로 검증 불가)

- 전면형: 게임오버/스테이지 전환 등에서 실제 광고가 뜨고 정상 종료되는지.
- 보상형: "광고 보고 부활" 시 광고 시청 완료 후 **보상(부활)이 실제 지급**되는지(`userEarnedReward` 수신).
- 두 ID가 이 앱의 올바른 광고 형식(전면/보상)에 매칭되는지 — 잘못 매칭 시 로드 실패/보상 실패 가능(Codex 지적).
- 샌드박스에서 운영 ID 노출 여부.

## 5. 관련 커밋

- `8d5beac` 보상형 운영 ID / `2bb664f` 전면형 운영 ID / `9c7c647` v14 세션문서
- 화면 수정(`20260711-3`에 담김): `b624756` 등 — 상세는 v13 참조
