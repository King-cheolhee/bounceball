# SESSION_V2 — 작업 기록 및 다음 세션 인수인계

> 작성일: 2026-06-13 · 이전 기록: SESSION_V1.md · 다음 세션 시작 시 이 문서를 먼저 읽을 것

---

## 1. 이번 세션(V2)에서 완료한 것

### 1-1. 카메라 멀미 조정 (사용자 피드백 해소) — 커밋 017f606
- 중앙 고정 추적식 → **데드존(카메라 윈도우) 방식**: 공이 화면 중앙 가로 ±25%·세로 ±34% 안이면 카메라 완전 정지, 벗어나면 가장자리 복귀까지만 이동
- 세로 데드존 ±245px > 점프 호 240px → **평지 바운스 중 카메라 상하 이동 0px** (시뮬레이션 실측)
- 추적 속도 하향(가로 0.10/세로 0.07), lookahead 제거, 하드 가드 8%(자유낙하·샤프트에서 공 화면 이탈 불가)
- 검증: `.tmp/camera-sim.cjs` 7항목 통과. **휴대폰 실기기 멀미 체감 확인은 미완 — 사용자 몫**
- 관련 상수(튜닝 시): `CAMERA_DEADZONE_X/Y`, `CAMERA_LERP_X/Y`, `CAMERA_HARD_EDGE` (`utils/constants.ts`)

### 1-2. 적대적 리뷰 hook — 발동 확인 + 경로 버그 2회 수정
- 발동 자체는 정상. 단 `${CLAUDE_PROJECT_DIR//\\//}` 치환이 이 환경에서 깨져 쓰레기 폴더 생성 → 상대 경로로 1차 수정 → **hook이 Bash 셸 cwd에서 실행됨이 드러나 상대 경로도 불안** → 최종: `cygpath -u` 절대 경로 (커밋 c03b589). 타 cwd 시뮬레이션 통과
- ⚠️ hook 설정은 세션 시작 시 스냅샷 — **다음 세션에서 (a) 쓰레기 폴더 재발 없는지 (b) Stop 리뷰 발동하는지 확인**

### 1-3. 상위 5개 게임 조사 → 구현계획서 → 사용자 승인 (C안 풀패키지)
- 멀티에이전트 16개 조사(4각도 랭킹→선정→심층→교차 검증): Red Ball 4 / 바운스볼 / Bounce Classic / Bounce Tales / BOUND 원작
- 산출물: `PLAN_V2_기믹확장.md`(설계 정본, 커밋 41004cf), `.tmp/리서치_상위5게임_종합.md`, `.tmp/리서치_상위5게임_원자료.json`
- 핵심 패턴: 입력 추가 금지(발판에 물리 위임) / 챕터당 신규 2종, 안전한 첫 등장 / 위험=도구 이중성 / 공정성 3원칙 / 점멸=흑백의 상태 문법

### 1-4. C안 풀패키지 구현 (커밋 373f8f8 → 30dd454 → 60d5f2c)
**기믹 8종 + 메타** (엔진: types/constants/blink.ts(신규)/Collision/Ball/GameEngine/Renderer/sound):
| 기믹 | 핵심 | 주요 상수 |
|------|------|-----------|
| 2회 벽돌 `brick` | 1회째 균열 시각화(점멸+금), 2회째 붕괴 | — |
| 폭탄+금간벽 | 점화→1.2s 점멸 가속→폭발: 반경170 벽 파괴, 반경320 수평 넉백 1.9×최고속. "등지면 길이 된다" | `BOMB_*` |
| 발사 패드 `launcher` | 착지 시 1.8×최고속 수평 발사, 반대키 2배 제동=꺾어 멈추기. **다른 발판과 수평 겹침 금지** | `LAUNCHER_SPEED_MULT` |
| 점멸 발판/벽 | 반주기=바운스주기×mult. on/warn/off/preview — 충돌·렌더가 `blink.ts` 공유 | `BLINK_WARN_MS` |
| 이동 가시 | 코사인 왕복, 주기=바운스주기×4 동기화, 경로 점선, 히트박스 75% | `MOVING_SPIKE_*` |
| 가짜 가시 `fake` | 진짜만 1.2s 주기 미세 플리커 | — |
| 추격전 `chase` | 등속 소멸 벽, 보호막 무효 `wave` 사망, 구덩이 금지 | S19 데이터 |
| 잔상 트레일 | 기본 상시(원작 오마주) + 설정 토글 | — |
| 완수 메타 | 스테이지별 부품 전량/노데스 기록(영속·상향만), 메뉴 세그먼트 3단 표기, 전 스테이지 PERFECT 시 엔딩 "YOU ARE A TRUE PIXEL." | `stage_records` |

**스테이지 개편** (stages.ts version 5):
- Ch1: S3/S4 벽반동 전용 ◆ 추가
- Ch2: S7 점멸 도입(보너스→편의→필수 3단), S8 발사 도입, S9 점멸+발사 조합(+신규 구덩이), S10 관문 종합
- Ch3: S11 폭탄 학습, **S12 「붕괴 시추공」 신규**(벽돌 위 시작→부수며 하강→하부 탈출, 900×2280), S13 유지, **S14 「격벽 폭파」 신규**(폭탄 2개: 학습→숙련), S15 관문(벽돌+샤프트 봉쇄 폭탄)
- Ch4: S16 이동 가시 도입, S17 가짜 가시+이동 가시, S18 벽돌 4개+brick 구덩이+이동 가시, **S19 「셧다운 웨이브」 신규**(추격전, 300px/s, 유예 2.5s), S20 점멸 발판+샤프트 점멸 벽
- 참고: 노미스 칭호 조건은 계획서의 "백업 셀 미사용"에서 **"전 스테이지 부품 전량+노데스"로 변경** (추적이 견고, 조건은 더 도전적) — 사용자에게 보고됨

### 1-5. 검증 (방법론 — 다음 세션에도 동일하게)
- 시뮬레이터: `validate2.cjs`(+금간벽-폭탄 짝·활공 착지·이동가시 침범), `climb-sim.cjs`(+S9 구덩이, S13 탈출구 좌표 동기화 — 낡은 좌표로 항상 FAIL이던 것 수정), **`gimmick-sim.cjs` 신규**(넉백 제동거리·등지기 생존·S12 하강 전선·S19 추격 여유·점멸 창), `camera-sim.cjs` 신규
- **시뮬레이터가 잡아낸 설계 결함**: S14 보상 벽 반사 사망, S14 가시밭 요행 착지(40px 틈), 카메라 세로 데드존 24px 부족 — 전부 수정
- 멀티에이전트 적대적 리뷰(5차원 리뷰어→회의적 검증자) 실행 — 결과는 §1-6
- 빌드 gzip ~73KB. git 커밋은 로컬만 (**사용자 결정: 원격 없이 유지**)

### 1-6. 적대적 리뷰 결과 (커밋 8bba9aa)
- 멀티에이전트 41개 (5차원 리뷰어 → 발견 건별 회의적 검증자): **확정 23건 전부 수정, 기각 13건** (전체 기록: `.tmp/리뷰_V2_확정.json`)
- 핵심 수정:
  - **physMs 물리 시계 신설**: 점멸·이동가시·웨이브·퓨즈·무적·반동 창의 기준 시계 — 30fps 서브스텝 양자화 + 히트스톱 중 세계 시계 누수 동시 해소. **앞으로 시간 기반 기믹은 반드시 physMs 사용** (timeMs는 인트로/사망 프리즈/히트스톱 등 연출 전용)
  - S20 점멸 벽 경계 사출 진동 (충돌 경계 가드 + 벽 위치 이동), wave vs 탈출구 동시 도달 플레이어 우대, 발판 높이 타이브레이크
  - 공정성: 넉백 반경(320) 점선 원 시각화, 가시 플리커 강화·전 가시 타입 통일('깜빡임=진짜'), S13 무입력 추락사(바닥 normal화)
  - 기록 무결성: 사망 계수 즉시화(onDeathCounted — 프리즈 중 재시도 익스플로잇 차단), stageDeaths 시도 단위 통일, 일시정지 메뉴 복귀 진행 보존(quitToMenu)
  - UX: 일시정지에 잔상 토글, 초기화 고지 문구, 무적 중 폭발 발판 비대칭
- 수용(미수정, 의도 문서화): 점멸 발판이 공 몸통에서 켜질 때 14px 이내 스냅(체감 미미), 이동 가시 자체 스윕 부재(현 파라미터 여유)
- gimmick-sim에 '전 스테이지 스폰 3초 무입력 생존' 자동 검사 추가 — 20/20 통과

---

## 2. 다음 세션(V3)에서 진행할 일

### 2-1. 실기기 검증 (최우선 — 사용자와 함께)
- [ ] 카메라 멀미 체감 (데드존 크기·추적 속도 — 상수 5개로 1분 튜닝 가능)
- [ ] 신규 기믹 손맛: 넉백 버티기 강도(`BOMB_KNOCKBACK_MULT` 1.9), 발사 속도(1.8), 점멸 주기(mult 2~3), 이동 가시 주기(mult 4), 추격 속도(S19 300px/s)
- [ ] S12 하강맵·S14 폭파맵·S19 추격전 실플레이 난이도
- [ ] 잔상 트레일 기본 ON이 어지럽지 않은지 (설정에서 끌 수 있음)

### 2-2. 잔여 확인
- [ ] hook 발동·쓰레기 폴더 재발 확인 (§1-2)
- [ ] 휴대폰 실기기 테스트 (Android/iOS)
- [ ] Phase 4: 앱인토스 콘솔 등록(appName 확정 필요), 실제 SDK 교체, 게임 등급분류(1~2주)
- [ ] 폰트 셀프호스팅 전환

## 3. 빠른 참조

```bash
cd app && npm run dev          # http://localhost:5173
cd app && npx tsc --noEmit && npm run build
# 스테이지·물리 변경 시 (프로젝트 루트):
cd app && npx esbuild src/game/stages/stages.ts --bundle --format=cjs --outfile=../.tmp/stages.cjs
node .tmp/validate2.cjs && node .tmp/climb-sim.cjs && node .tmp/gimmick-sim.cjs
node .tmp/verify-browser.cjs && node .tmp/camera-sim.cjs
```

- 작업본 `app/` (정본) · 설계 정본 `PLAN_V2_기믹확장.md` · 규칙 `AGENTS.md` (V2 기믹 규칙 포함)
