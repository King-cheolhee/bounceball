# 탱탱볼해금 SESSION v12 — 실기기 화면 잘림 대응 완성, 새 버전 등록 대기

> 작성일: 2026-07-11 · 이전: `탱탱볼해금_session_v11.md` · 다음 세션은 **이 문서를 먼저** 읽을 것
> 요지: 사용자가 실기기 스크린샷으로 "시작 버튼이 안 보인다"를 재확인 — 원인은 ① 토스 상단 바가 상시 세로 공간을 차지해 메뉴가 화면보다 길어짐 ② **실기기에는 수정 전 버전이 배포돼 있음**(오늘 수정은 아직 미배포). 메뉴를 낮은 화면에 맞게 압축했고, `.ait` 번들까지 빌드 완료. **콘솔 새 버전 등록·심사 제출은 사용자 승인 대기.**

## 0. 이번에 한 일 (v11 이후)

| 커밋 | 내용 |
|---|---|
| `0a3e01a` | visualViewport 감지 + 밀림 자동 복구 + 메뉴/설정 스크롤 허용 (v11) |
| `47e6c08` | Codex P1: offset형 밀림도 #root 이동으로 보정 |
| `cb28c08` | 실기기 대응 메뉴 압축 + Codex P1 2건: ① offset 보정 시 #root 크기도 일치 ② 좌/우 터치 판정을 rect 기준으로 |

메뉴 압축 내용(`MainMenuPage.tsx`): 세로 여백·스테이지 숫자를 화면 높이 비례 축소(`clamp`/`min`+vh), 우상단 버튼 세로 스택 → 한 줄, 등급 표시는 좌하단 `position: fixed` 상시 고정(게임법 §33 초기화면 표시 유지).

검증: `tsc`+vite 빌드 통과. headless Chrome 844×280(극한)에서 시작 버튼·스테이지 선택 모두 표시, 844×390에서 스크롤 없이 전체 표시, 게임 실행·입력·캔버스 리사이즈 정상, 콘솔 오류 0.

## 1. 배포 상태 (중요)

- **실기기의 현재 버전(20260710-2)에는 오늘 수정이 없다.** 코드 수정 → 실기기 반영은 반드시 [콘솔 새 버전 등록 → 심사 → 출시] 경로.
- `.ait` 번들 빌드 완료: `app/tangtangball.ait` (deploymentId `019f4cdc-a875-7503-9394-99eef25c12ad`)
- **다음 행동: 사용자 승인 후 콘솔에 새 버전 등록 + 검토 요청 제출.**

## 2. 남은 일

1. (승인 시) 콘솔 새 버전 등록·심사 제출 — 화면 수정만 먼저.
2. 광고: 콘솔 **광고 승인 대기 중**. 승인 나면 실제 adGroupId 2개(전면형/보상형)를 `app/src/services/ads.ts:21-24`에 교체 후 또 다른 새 버전으로.
3. 새 버전 실기기 확인 항목: 상단 바 있어도 시작 버튼 보임 / 메뉴·설정 스크롤 / 게임 좌우 터치 정상(판정 로직 변경됨).

## 3. 주의사항

- 실기기 미검증 항목: visualViewport offset형 밀림 보정(데스크톱 재현 불가), 터치 rect 판정 — 심사용 샌드박스/실기기에서 꼭 확인.
- Codex P2(미적용): 뷰포트 훅 중복 사용 정리, 연속 리사이즈 rAF 병합 — 실기기에서 프레임 끊김 체감 시 적용.
- 이전 심사(20260710-2)가 아직 진행 중이면 새 버전 등록 방식(교체/추가)을 콘솔 정책대로 따르고, 기존 요청 임의 취소 금지.
